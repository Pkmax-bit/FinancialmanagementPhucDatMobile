<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base Button Style -->
    <style name="Widget.App.Button" parent="Widget.Material3.Button">
        <item name="android:textAppearance">@style/TextAppearance.App.LabelLarge</item>
        <item name="android:minHeight">@dimen/button_height</item>
        <item name="cornerRadius">24dp</item>
        <item name="android:paddingStart">@dimen/button_padding_horizontal</item>
        <item name="android:paddingEnd">@dimen/button_padding_horizontal</item>
        <item name="android:textAllCaps">false</item>
    </style>
    
    <!-- Primary Button (Filled) -->
    <style name="Widget.App.Button.Primary" parent="Widget.App.Button">
        <item name="backgroundTint">@color/md_primary</item>
        <item name="android:textColor">@color/md_on_primary</item>
        <item name="elevation">@dimen/card_elevation</item>
    </style>
    
    <!-- Secondary Button (Tonal) -->
    <style name="Widget.App.Button.Secondary" parent="Widget.Material3.Button.TonalButton">
        <item name="android:textAppearance">@style/TextAppearance.App.LabelLarge</item>
        <item name="android:minHeight">@dimen/button_height</item>
        <item name="cornerRadius">24dp</item>
        <item name="backgroundTint">@color/md_secondary</item>
        <item name="android:textColor">@color/md_on_secondary</item>
    </style>
    
    <!-- Outlined Button -->
    <style name="Widget.App.Button.Outlined" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:textAppearance">@style/TextAppearance.App.LabelLarge</item>
        <item name="android:minHeight">@dimen/button_height</item>
        <item name="cornerRadius">24dp</item>
        <item name="android:textColor">@color/md_primary</item>
        <item name="strokeColor">@color/md_primary</item>
        <item name="strokeWidth">1dp</item>
    </style>
    
    <!-- Text Button -->
    <style name="Widget.App.Button.Text" parent="Widget.Material3.Button.TextButton">
        <item name="android:textAppearance">@style/TextAppearance.App.LabelLarge</item>
        <item name="android:textColor">@color/md_primary</item>
    </style>
    
    <!-- Success Button -->
    <style name="Widget.App.Button.Success" parent="Widget.App.Button">
        <item name="backgroundTint">@color/md_success</item>
        <item name="android:textColor">@android:color/white</item>
    </style>
    
    <!-- Error Button -->
    <style name="Widget.App.Button.Error" parent="Widget.App.Button">
        <item name="backgroundTint">@color/md_error</item>
        <item name="android:textColor">@android:color/white</item>
    </style>
    
    <!-- Small Button -->
    <style name="Widget.App.Button.Small" parent="Widget.App.Button">
        <item name="android:minHeight">40dp</item>
        <item name="android:textSize">12sp</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
    </style>
</resources>
